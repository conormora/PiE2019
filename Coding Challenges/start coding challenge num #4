d = {'hello': 1, "goodbye" :2}
print(d['hello'])


def rotate(num):
    largest = 0   
    #First convert num to a string 
    #find length of string (this corresponds to number of digits in num) len()
    #use a loop to find the largest digit in num (for or while loop)
    
    while(condition):# condition are true or false statements
        body
        
    x = 0
    while (x < 5):
        print(x)
        x = x + 1
    
    s=str(num) #this is a stirng You put s=str(x)

num_of_variable = len(s)   #This is the number of digits in number

    while(num_of_variable > 0)
     if largest < num_of_variable%10 
    largest = num_of_variable%10
    num_of_variable=num_of_variable//10 
    largest
